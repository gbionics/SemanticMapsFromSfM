# Semantic Maps from Images 


ToDo's
- We are assuming a sfm reconstruction is already available, should include code to automatically generate it from images
- The masks are generated with DeepLabv3 for now, but we should replace it with something better. SAM3 on video input?